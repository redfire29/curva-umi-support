---
import Layout from "../layouts/Layout.astro";
import IndexContent from "../components/pages/Index.svelte";
import { ui } from "../i18n/ui";

const locale = "ja";
const messages = ui[locale];
---

<Layout title="Curva Umi Support">
  <IndexContent client:idle locale={locale} messages={messages} />
</Layout>
